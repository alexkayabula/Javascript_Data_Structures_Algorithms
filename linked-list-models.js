export class Node {
    constructor(element){
        this.element = element;       // value we want to add to the list
        this.next = undefined;        // pointer that contains a link to the next item
    }
}